__pycache__/
*.pyc
*.pyo
.env
.venv/
venv/
.index/
.pytest_cache/
*.egg-info/
dist/
build/
*.log
# Generated projects + user data (brain/users/...) and local engine settings - never published
brain/
config/settings.yaml
test_diagram.svg
reseed.py

# NOTE: plan.md, resume.md, docs.md are intentionally TRACKED for now - the author works
# across multiple laptops and syncs them via git. Move them here only at open-source time.

# Playwright MCP browser artifacts (CI debugging)
.playwright-mcp/
