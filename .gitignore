__pycache__/
*.pyc
*.pyo
.env
.venv/
venv/
.index/
.pytest_cache/
*.egg-info/
dist/
build/
*.log
# Generated projects + user data (brain/users/...) and local engine settings - never published
brain/
config/settings.yaml
test_diagram.svg
reseed.py

# Local working docs: the design spec + the session journal stay private (durable decisions
# also live in code + CHANGELOG.md). NOTE: both were tracked until now, so they remain in the
# existing git history - a scrub is needed to remove them from the published repo, and
# README/CONTRIBUTING linked to plan.md.
plan.md
resume.md

# Playwright MCP browser artifacts (CI debugging)
.playwright-mcp/
