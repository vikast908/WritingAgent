# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/

# Tooling caches
.pytest_cache/
.ruff_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/

# Local environment / editor
.env
.claude/
*.log

# Playwright MCP browser artifacts (CI debugging)
.playwright-mcp/

# Runtime state (brain/, .index/, config/) lives in the agent home - see
# src/writingagent/paths.py ($WRITINGAGENT_HOME, default: the OS user-data dir) -
# so the app never writes into the repo.

# The docs site (website/) is its OWN nested git repo (remote: vikast908/writingagentdocs),
# committed and deployed (Vercel) independently. Never track it here.
/website/

# Maintainer session journal - kept local, never published
docs/dev/resume.md

# Impeccable skill critique snapshots (local tool state)
.impeccable/
