__pycache__/
*.pyc
*.pyo
.env
.venv/
venv/
.index/
.pytest_cache/
.ruff_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/
.claude/
*.egg-info/
dist/
build/
*.log
# Generated projects + user data (brain/users/...) and local engine settings - never published
brain/
config/settings.yaml
test_diagram.svg
reseed.py

# NOTE: the maintainer journals moved to docs/dev/ (resume.md = session log, test.md =
# verification log) at open-source time. They stay TRACKED (synced across machines via git);
# plan.md (architecture spec) stays at the repo root.

# Playwright MCP browser artifacts (CI debugging)
.playwright-mcp/

# Docs site lives in its OWN nested git repo (remote: vikast908/writingagentdocs)
# and is committed/managed independently. The WritingAgent repo must NOT track it.
# Keep this ignore so the two repos stay separate but sit side-by-side on disk.
/Writing Agents docs/

# Maintainer session journal - kept local, never published
docs/dev/resume.md
