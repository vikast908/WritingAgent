# NOTE: pyproject.toml is the canonical dependency list - prefer `pip install -e .`
# (or `pip install -e ".[dev]"`). This file mirrors the runtime deps for convenience.
# LLM calls go through OpenRouter (OpenAI-compatible API).
openai>=1.0
pydantic>=2.0
pyyaml>=6.0
python-dotenv>=1.0
markdown>=3.5
xhtml2pdf>=0.2.11
rich>=13.0
pyfiglet>=1.0
prompt_toolkit>=3.0
ddgs>=0.1                 # web search for the Researcher node (plan §4); no API key needed
ebooklib>=0.18            # EPUB export (book export --format epub)
# Optional: semantic skill retrieval (plan §10). Enable use_embeddings in settings.yaml.
# Downloads ~80 MB all-MiniLM-L6-v2 on first use; falls back to lexical if not installed.
# sentence-transformers>=2.0

# Optional: deep multi-source researcher fetch backend (plan §15.2). Enable
# `deep_research` in settings.yaml. deep_research.py imports Scrapo lazily and falls
# back to a pure-stdlib urllib + html.parser fetch when it's absent, so this is never
# required. Scrapo needs Python 3.11+ and installs from git (it is not on PyPI):
#   pip install ".[deep]"                          # via the pyproject extra, or directly:
#   pip install "git+https://github.com/vikast908/Scrapo"
# For Scrapo's browser tier (renders JS-heavy / bot-blocked pages the HTTP tiers can't),
# also install Playwright and its Chromium binary:
#   pip install playwright
#   python -m playwright install chromium
#
# For an exact, reproducible environment: `pip freeze > requirements.lock.txt` after a
# known-good install (no lock file is checked in - see CONTRIBUTING.md).
